# WHOOP без OpenAI: проверка обновления дашборда

TL;DR: существующий [дашборд WHOOP](http://127.0.0.1:53000/dashboard/3) обновлён: семь отдельных графиков и текущий вес. Изменения проверены на тестовых и реальных данных без OpenAI. Медицинские записи и подключения не менялись. Это не объявление готовности всей v0.1 или AI-интерпретации.

## До обновления

- Локальные PostgreSQL и Metabase v0.63.13 запущены; `/healthcheck` отвечает HTTP 200.
- WHOOP dashboard ID 3, пять существующих карточек ID 41–45. Пользовательских дополнительных карточек в нём не найдено. Дашборд анализов ID 2 и демонстрационные объекты Metabase не входят в изменение.
- SQL старых карточек: recovery 533 строки / 432 дня; HRV 532 / 431; основной сон 532 / 431; вес 1 строка. Это подтверждает, что устранение неоднозначных дневных точек необходимо на реальных данных.
- Браузер авторизован локально; адрес `/dashboard/3`, заголовок WHOOP. Секреты и медицинские значения не включены в отчёт.
- Облачное извлечение выключено: запросов сегодня 0; 267 страниц ожидают облачной обработки, 6 требуют внимания. Неизвестные результаты предыдущих запросов не перезапускались.

## Приёмка

- Реализация `6b4759a`, исправление `8af6503`, объединённая сборка `e5d996d`.
- 21 профильный тест прошёл. В том числе реальные SQL-запросы на отдельной PostgreSQL с синтетическими данными: профили с одинаковыми внешними ID, дубли дней, невалидная последняя запись, пропуски, неконечные значения, naps, будущее и снимок веса.
- Независимое ревью выявило небезопасное принятие карточки только по имени; дополнительно исправлена совместимость старых имён default-профиля. Повторное ревью: обе проблемы устранены, новых важных дефектов нет.
- Полный прогон `uv run pytest -q`: **921 passed**, 5 прежних SWIG-предупреждений, 13.79s. Ruff чистый; mypy — 107 файлов без ошибок; `uv lock --check` и `git diff --check` успешны.
- Проверка новых SQL на реальной базе без изменений: все восемь запросов выполнились; временные ряды дают одну строку на день, вес — одну строку. Значения в отчёт не включены.

- Финальное независимое ревью всего изменения `61fe712..e5d996d`: **READY**, важных и блокирующих замечаний нет. Прежние SWIG-предупреждения признаны неблокирующими.
- Живая команда `uv run health-agent dashboard setup-whoop --profile-id 00000000-0000-0000-0000-000000000001` завершилась `status=ready`, dashboard ID 3, восемь карточек. Сохранены все пять исходных ID 41–45; добавлены 46–48.
- Повторная живая настройка сохранила ID, адрес дашборда и общее количество карточек. Дублей не создано.
- Каждый из восьми запросов выполнен через Metabase `/api/card/{id}/query`: все `completed`. Семь временных рядов возвращают `date` и один показатель; вес — одну строку с весом и временем получения. У strain 432 дневные точки, у остальных рядов 431.
- Настоящий браузер на `/dashboard/3`: восемь карточек, все ожидаемые названия, заголовки таблицы «Вес, кг» и «Получено из WHOOP», ошибок отображения и HTTP-ошибок ресурсов нет. При ширине 1440px ширина страницы 1440px, наложений и карточек нулевого размера нет. Консоль: 0 ошибок, 1 неклассифицированное предупреждение Metabase; оно не сопровождалось отказом запроса или отображения, исправление стороннего интерфейса не выполнялось.
- `/healthcheck` после установки снова отвечает HTTP 200. Повторные авторизации пользователя, запросы OpenAI и тестовые записи в рабочей медицинской базе не потребовались.

## Что осталось

- Анализы крови: отдельные многолетние графики, проверка показателей и восстановление медицинских дат. [Локальный аудит](2026-09-06-local-import-audit.md) нашёл причину отсутствующих дат и кандидатов для 32 документов; это предложения, а не уже сохранённые даты. Никакие 584 ожидающих проверки показателя автоматически не подтверждались.
- Сквозные сценарии v0.1 и качество живых AI-ответов не объявляются завершёнными. Облачное извлечение остаётся выключенным; восстановление баланса не требуется для уже установленных графиков.
- Полная история реализации/исправлений сохранена в git и этом отчёте. Временные пакеты заданий и ревью данного завершённого этапа можно убрать из рабочей папки; другие планы и рабочие деревья не затрагиваются.
