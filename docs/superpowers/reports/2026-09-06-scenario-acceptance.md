# Five-scenario acceptance — 2026-09-06

Audited `codex/v1-slice-1`, HEAD `d68d0f4`. This is a focused acceptance check, not a repeat readiness audit. Existing synthetic components pass; none of the five complete product scenarios is yet demonstrated end to end with the selected Yandex provider and reviewed local data.

`PASS` means the complete scenario is demonstrated; `PARTIAL` means useful implemented components pass but a required acceptance case is missing; `BLOCKED` means the core workflow is absent or unavailable. Missing cases below are proposed tests, not observed pytest failures.

| Scenario | Status | Exact evidence | Smallest next code task / missing acceptance case | External needs |
|---|---|---|---|---|
| 1. Health picture from labs + WHOOP | PARTIAL | `tests/questions/test_question_loop_integration.py::test_bound_telegram_question_uses_only_profile_scoped_cited_evidence` passes with both sources and a mocked responder; `tests/test_insights.py` covers lab gaps, source ranges and wearable windows. Production composition still constructs `OpenAIResponsesResponder` in `questions/composition.py:440`. | Add provider selection and a Yandex responder through the existing responder interface; reuse the combined-source fixture with a mocked Yandex transport and require both citations, profile isolation and an explicit gap when no labs are verified. | Yandex credentials/project/model configuration and synthetic live smoke. Owner review of lab observations and medical dates; the inherited audit has zero verified observations. |
| 2. What to check / do next | PARTIAL | `insights/catalog.py` has sourced `possible_next_step` text; snapshot tests cover unknown ranges and missing evidence, and question-service tests reject forged citations and unsafe inference. Current next steps are general explanations, not a demonstrated personalized checkup workflow. | Add one acceptance fixture for “what should I check next?” with an abnormal verified lab, a wearable trend and a missing-data gap. Assert a cited, concrete next step for the available evidence and clear missing-data language; wire the selected provider as in scenario 1. | Same provider configuration and reviewed evidence as scenario 1. A screening schedule, if requested, needs explicit user/clinician context rather than inferred intervals. |
| 3. Recurring checkup reminders | PARTIAL | `tests/reminders/test_integration.py::test_full_confirmed_reminder_flow_survives_dispatch_cycles_and_isolates_profiles` passes proposal → confirmation → delivery → snooze → completion. `reminders/models.py` stores one `due_at`; `reminders/repository.py:172` completes the row without creating a successor. | Add an explicit recurrence interval and an idempotent next-occurrence transition. Acceptance: complete a confirmed recurring checkup, restart/replay completion, and deliver exactly one next occurrence at the agreed local time; cancellation stops recurrence. | Owner-selected checkup interval and timezone; working Telegram binding and reminder agent for live delivery. Credentials for an LLM are unnecessary for this deterministic flow. |
| 4. Doctor visit: calendar event, questions, saved answers | BLOCKED | No calendar/visit workflow or persistence model appears in `src/health_agent`, migrations or tests. `gmail/message_inbox.py:15` only retains appointment source provenance. `tests/gmail/test_service.py::test_body_only_appointment_is_retained_as_attention` documents the current handoff boundary; it does not create a visit. | Implement a minimal profile-scoped visit record with time/timezone, questions and saved answers, plus an idempotent calendar adapter. Acceptance with a fake calendar: create visit → create one event → save questions/answers → restart and retrieve them; retries must not duplicate the event. | Calendar provider/target selection and its authorization; real visit details. These are separate from the Yandex text-model choice. |
| 5. New Drive/Gmail/Telegram document → DB → review → Sheets/graphs | PARTIAL | Drive `test_pdf_enters_database_review_and_remains_profile_isolated`, Gmail `test_medical_pdf_enters_common_database_and_review_pipeline`, Telegram `test_production_photo_review_confirm_429_restart_question`, Sheets `test_profile_sheet_roundtrip_isolated_and_idempotent`, and `test_synthetic_pdf_reaches_verified_history_once` all pass in their respective test files. They prove separate legs, not one combined path for each source. | Add a parameterized synthetic acceptance test per ingress: dated document → pending DB row → explicit review → fake Sheets projection + actual chart query; repeat ingress/review/sync and assert no duplicate publication. Include an undated fixture that stays off the dated chart until date review. Implement conservative date proposals/backfill using the corrected local date audit. | Existing source authorizations and one synthetic live roundtrip to the configured Sheet/dashboard. Real observations/dates need owner review. Yandex is needed only for extraction cases that require the cloud fallback; `lab_extraction/service.py:57` currently constructs an OpenAI extractor. |

Inherited data blocker: [the corrected local import audit](2026-09-06-local-import-audit.md) reports 121 documents without collection/issue dates, 584 pending observations and zero verified observations. Its corrected date probe identifies 15 collection candidates, 16 study-performed dates and one result-ready date; study/readiness dates must not be treated as collection/issue dates. These counts were not re-queried here. Reviewing observations alone does not restore dated charts.

Verification:

```sh
uv run pytest -q tests/test_insights.py tests/questions/test_context.py tests/questions/test_service.py tests/questions/test_question_loop_integration.py tests/reminders/test_integration.py tests/telegram/test_capture_integration.py tests/google_sheets/test_integration.py tests/google_drive/test_medical_consumer.py tests/gmail/test_medical_importer.py tests/test_slice_1_e2e.py
```

Result: **71 passed**, 5 existing dependency deprecation warnings, 3.67 seconds. The appointment-attention test was inspected, not included in this run. Tests use synthetic documents, injected fake gateways/responders, temporary local files and the UUID-guarded disposable PostgreSQL fixture in `tests/conftest.py`; fixture cleanup targets only its own test container. No production database access, cloud requests, external messages, real medical text, credentials or operational configuration changes were used in this audit. Only this report was added.
